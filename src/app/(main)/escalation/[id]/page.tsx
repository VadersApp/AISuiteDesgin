// This page has been moved to /dashboard/system-alerts/[alertId]
// and is no longer in use. It can be safely deleted.
import { redirect } from 'next/navigation';

export default function DeprecatedEscalationPage() {
    redirect('/dashboard');
}
